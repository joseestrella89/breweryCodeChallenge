import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        padding: 12,
    },
    input: {
        flex: 1,
        marginHorizontal: 8,
    },
});

export default Styles;
